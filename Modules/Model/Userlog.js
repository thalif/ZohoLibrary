export default class UserLog
{
    constructor(userName, userFullName, gender, logTime)
    {
        this.UserName = userName;
        this.FullName = userFullName;
        this.Gender = gender;
        this.LogTime = logTime;
    }
}